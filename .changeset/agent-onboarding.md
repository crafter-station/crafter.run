---
"@crafter/cli": minor
---

Add agent-friendly onboarding: `crafter onboard --file <json-file> --confirm` creates or updates a Crafter profile from the terminal, and `crafter handle <handle>` checks handle availability before you claim one.
